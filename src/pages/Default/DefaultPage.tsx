import { FC } from 'react'

import DefaultComponent from '../../components/Default/Default'

const LABEL = '01 - Default component in React'

const DefaultPage: FC = () => <DefaultComponent label={LABEL} />

export default DefaultPage
